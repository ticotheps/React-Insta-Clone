import React from 'react';

const SearchBar = () => {
    return (
        <div className="search-bar">
            <div>
                <i class="fab fa-instagram"></i>
            </div>
        </div>
    );
};

export default SearchBar;